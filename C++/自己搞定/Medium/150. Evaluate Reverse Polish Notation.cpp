#include<iostream>
#include <bits/stdc++.h>
#include <map>
using namespace std;


class Solution {
public:
    int evalRPN(vector<string>& tokens) {
        stack<std::string> answer;
        for(std::string word :tokens){
            if(word=="+"||word=="-"||word=="/"||word=="*"){
                string number =answer.pop()
            }
            answer.
        }
        
    }
};